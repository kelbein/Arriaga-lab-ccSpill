% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_solution_data.R
\name{load_solution_data}
\alias{load_solution_data}
\title{Load and Process All Solution Mode Data}
\usage{
load_solution_data(
  base_data_dir,
  conc_lookup_data,
  valid_isotope_names,
  renaming_function = rename_as_isotopes
)
}
\arguments{
\item{base_data_dir}{The top-level path to the data.}

\item{conc_lookup_data}{The metadata lookup table. This should be the
\verb{$lookup_table} object returned from your \code{concentration_lookup()} function.}

\item{valid_isotope_names}{A character vector of valid isotope names.
This should be the \verb{$valid_isotopes} object from \code{concentration_lookup()}.}

\item{renaming_function}{The function to use for renaming columns. This
should be \code{rename_as_isotopes} (or another function you can defined).}
}
\value{
A nested list of data frames, structured by date and data type
(e.g., \code{all_data$'date-1'$'isotope-1'}).
}
\description{
This is the main data loading function. It walks a nested directory
structure (Date > DataType > Level), reads all raw .txt files,
calculates the mean for each, and joins the results with metadata.
}
\details{
This function requires the 'dplyr' package.
}
\examples{
\dontrun{
  # 1. First, get the metadata
  meta_path <- "path/to/your/meta-data.csv"
  meta <- concentration_lookup(meta_path)

  # 2. Define the data directory
  data_dir <- "path/to/your/ccSpill/solution-mode-data"

  # 3. Load all the data
  all_data <- load_solution_data(
    base_data_dir = data_dir,
    conc_lookup_data = meta$lookup_table,
    valid_isotope_names = meta$valid_isotopes,
    renaming_function = rename_as_isotopes
  )
}

}
